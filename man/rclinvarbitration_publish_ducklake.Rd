% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ducklake.R
\name{rclinvarbitration_publish_ducklake}
\alias{rclinvarbitration_publish_ducklake}
\title{Publish a tidy ClinVar evidence export to DuckLake}
\usage{
rclinvarbitration_publish_ducklake(
  con,
  export,
  table_name = "clinvar",
  staging_table = "clinvar_incoming",
  ducklake_name = NULL,
  author = "RClinVarbitration",
  commit_message = NULL
)
}
\arguments{
\item{con}{A DuckDB DBI connection whose current database is the writable
DuckLake catalog.}

\item{export}{The returned value from
\code{\link[=rclinvarbitration_export_clinvarbitration_parquet]{rclinvarbitration_export_clinvarbitration_parquet()}} with
\code{schema = "tidy"}, or the returned value from
\code{\link[=rclinvarbitration_import_flat]{rclinvarbitration_import_flat()}} when \code{parquet_path} was supplied.}

\item{table_name}{Persistent current-decision table.}

\item{staging_table}{Persistent empty staging table.}

\item{ducklake_name}{Attached DuckLake catalog. \code{NULL} uses the current
database.}

\item{author}{Snapshot author.}

\item{commit_message}{Snapshot message. \code{NULL} derives one from the export.}
}
\value{
Invisibly returns the publication snapshot, source identity, input
row count, and DuckLake change counts.
}
\description{
Registers one tidy Parquet export in a persistent staging table, then
replaces the current evidence set by key in one DuckLake transaction.
New keys are inserted, absent keys are deleted, changed content is updated,
and byte-identical records are left untouched. DuckLake's native data-change
feed is the release-delta authority.
}
\details{
The staging and target tables are initialized once from the Parquet schema.
The staging table is retained because current DuckLake releases cannot
create, register, and drop that table safely in the same transaction. It is
empty before and after every successful publication.
}
